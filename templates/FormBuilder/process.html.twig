{% extends 'base.html.twig' %}

{% block title %} {{ form_builder.legend }}{% endblock %}


{% block content %}
    {{ form_start(form) }}

    {{ form_widget(form) }}

    <div class="form-group">
        <div class="col-md-offset-2 col-md-9">
            <button type="submit">Submit</button>
        </div>
    </div>

    {{ form_end(form) }}
{% endblock %}


